// ====================================================
// IMPORTS
// Main
import axios from 'axios'

// ====================================================
// Instance

const api = {
	key: 'key',
}

// ====================================================
// Requests

export const API = {
	// getAddress: position => {
	// 	return axios.get(
	// 		`${api.base}?apikey=${api.key}&geocode=${position}&format=json&lang=en_US`
	// 	)
	// },
}
